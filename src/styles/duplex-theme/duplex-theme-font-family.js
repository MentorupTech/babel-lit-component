import { css } from "lit-element";
const fonts = css`
  :host,
  html {
    /** FONT FAMILY VARS */
    --duplex-fontDefault: "Raleway", sans-serif;
    --duplex-fontSecondary: var(--duplex-fontDefault);
  }
`;

export { fonts };
